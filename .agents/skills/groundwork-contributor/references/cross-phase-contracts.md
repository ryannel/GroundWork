# Cross-Phase Contracts

Part of the `groundwork-contributor` skill. Read this before any cross-cutting
restructure — the chains below are where single-phase assumptions hide.

The phases communicate through shared artifacts and identifiers. Each is written by one
skill and read by others — usually in different files, often in different sessions — so a
change to any writer must update every reader in the same change (`skill-writer` names the
failure mode: identifier drift is silent; a consumer reading a stale shape finds nothing,
with no error). This table is the map of those chains.

| Artifact / identifier | Written by | Read by |
|---|---|---|
| `interface_type` → the interface track recorded in `docs/design-system.md` | design-system Step 2 (greenfield); design-system-extract Stage 1 (brownfield) | design-system track loader; scaffold Phase 1 / infra-adopt Phase 2 (surface registry → system-test-runner `--surfaces` deps + fixtures); bet design + `groundwork-bet/references/bet-progress-tests.md` (vocabulary, test medium) |
| Pitch `status` frontmatter (`docs/bets/<slug>/pitch.md`) | each bet workflow on phase entry | bet activation routing; orchestrator state reconciliation; groundwork-check |
| Pitch `track: quick` frontmatter (`docs/bets/<slug>/pitch.md`) — marks the **quick-bet** lane, the middle of three delivery depths (patch · quick bet · bet). Set by the orchestrator's Work Intake triage and the quick-bet track | bet `workflows/00-quick.md` (the quick-bet track: collapses discovery+design+decomposition into one approved pass producing a single milestone) | bet activation (resume `status: quick` → 00-quick; `lane: quick-bet` from the orchestrator → 00-quick); 04-delivery (milestone-close scopes down: no experience-auditor subagent / Tier-3, UX floor kept); 05-validation (skips the cross-slice retrospective + per-decision ADRs); the decomposition checklist (one-milestone count is legal); groundwork-update Bets family |
| `docs/bets/<slug>/technical-design/` prose (API/data design carries the shapes at design fidelity — no separate spec files) | bet 02-design | 03-decomposition (proof-of-work shapes); 04-delivery (code-first build); 05-validation (verification) |
| `docs/architecture/api/<service>/` canonical specs (captured code-first from the running service) | 05-validation capture (snapshot of each touched service's served contract) | the generated contract-conformance system tests (run by the project's `./dev test`); groundwork-check |
| `docs/bets/<slug>/decomposition/` prose tree (milestone `index.md` + slice files, each with a **Proof of work** section) + the git tag `bet/<slug>/approved` on the approved-prose commit | 03-decomposition (tree + approval tag); amendments re-commit and re-tag | 04-delivery (red suite materialized from it; per-slice prose tamper-check vs the tag); implementation-readiness gate; 05-validation reconciliation |
| `.groundwork/config/brand-tokens.json` | design-system commit (every track) | scaffold → `workspace-dev-cli` theming (`.dev/dev.config.json`) |
| `docs/architecture/infrastructure.md` | scaffold Phases 4–6; infra-adopt | bet 03-decomposition (test language, service names); delivery |
| Downstream Context files (`.groundwork/context/<phase>.md`) + hand-off cache (`.groundwork/cache/handoff/<phase>.md`) | each setup phase's commit (Protocols 5–6) | the next setup phases' inits; the hand-off is single-hop (deleted when consumed), the context store is torn down at Setup Graduation (Protocol 10). Published `docs/` carry no summary section |
| Discovery-notes headers — the **five** canonical headers (`## Product Brief`, `## Design System`, `## Architecture`, `## Design Details`, `## Bets`) in `.groundwork/cache/discovery-notes.md` and the shared `templates/discovery-notes.md` | any phase, on out-of-phase signals (Protocol 1) | the phase that owns the matching header, at its init or design step |
| `docs/architecture/index.md` §3 Capability Ports & Providers + `.groundwork/capability-ports.json` (each technical port → provider → footprint) | architecture Phase 7 commit (step 5b); architecture-extract commit | scaffold Phase 1 (port → generator flag / `add-capability` invocation); scaffold Phase 2 (footprint registration check); scaffold Phase 4 (reconcile footprints — compose service / runner / env / `none`-xfail); bet delivery (a `none` raw gateway is the bet) |
| Screenshot capture path `.groundwork/cache/visual/<bet-slug>/<surface>/<state>.png` (+ `_smoke/<surface>/<route>__<viewport>__<theme>.png`) | system-test-runner render-smoke (`_smoke` set); bet-progress interface tests (per-state set) | 04-delivery Tier 2 spec-conformance inspection (delivery agent); 05-validation confirms it ran |
| `## Design References` (technique library) in `docs/design-system.md` + `references` array in `brand-tokens.json` `visual` block | design-system commit (_foundation Phase 6 convergent technique-research pass + graphical-ui Commit Contributions); design-system-extract | the atmosphere/motion token projection + the per-surface micro-polish spec at bet design |
| `visual` block atmosphere tokens (`elevation`, `blur`, `gradients`, `surface`, `motion.interactions`, `typography.roles`) in `brand-tokens.json` | design-system commit (graphical-ui Commit Contributions) | nextjs-app generator projection → `app/brand.css` + `globals.css` token utilities; `tests/system/test_token_conformance.py` (Tier 1) |
| Per-screen micro-polish spec in the bet's `technical-design/01-ui-design.md` UI Design (motion / atmosphere / static-micro, token-traceable) | bet 02-design Step 1.95 (designer persona); concreteness-gated at Step 2.5 | 04-delivery Tier 2 spec-conformance inspection; 05-validation |
| Scan findings slices (`.groundwork/cache/scan/{overview,product-findings,design-findings,architecture-findings}.md`) + `.groundwork/cache/scan-state.json` | `groundwork-scan` Stage 3a/3b (per-partition digests + status); Stage 4 finalizes `overview.md` and sets `scan-state.json` `status: complete` | `groundwork-product-brief-extract` (`product-findings.md`; deletes it once consumed), `groundwork-design-system-extract` (`design-findings.md`), `groundwork-architecture-extract` (`architecture-findings.md`) — each also reads `overview.md` + `scan-state.json`; `groundwork-infra-adopt` reads `overview.md` + `scan-state.json` as the last brownfield phase and owns the teardown of the shared scan cache (including `scan-state.json`) at its own commit |
| `.groundwork/cache/gap-ledger.md` (template: `.groundwork/skills/templates/gap-ledger.md`) | `groundwork-product-brief-extract`, `groundwork-design-system-extract`, `groundwork-architecture-extract` (heaviest contribution — every missing machine-readable contract at blocks-delivery severity) each append phase-specific divergences from standard; `groundwork-infra-adopt` also appends a row when the existing-service count disagrees with the architecture doc | `groundwork-infra-adopt` Phase 5 (Roadmap step) — converts every entry into a `docs/maturity.md` roadmap row (gap, dimension D1–D9, severity, recommendation, status, evidence), then deletes the working ledger file once its entries live in `docs/maturity.md` |
| `.groundwork/cache/repo-map.json` (schema: `src/hidden-skills/repo-map-schema.md`) | `npx groundwork-method repo-map` (the deterministic tree-sitter generator — parses, resolves import edges, ranks by centrality) or, when unsupported, LLM-inference filling the same shape | `groundwork-scan` Stage 3 (centrality ranking to prioritize scan depth per partition); `groundwork-architecture-extract` (exact dependency facts); `groundwork-doc-sync` and `groundwork-check` (impact analysis + staleness via `generated_at_commit`) — `groundwork-infra-adopt` explicitly **preserves** this file at scan-cache teardown because `groundwork-check` and the bet loop keep reusing it after setup |
| Discipline-persona reference filenames (`references/*.md` under `groundwork-architect/`, `groundwork-product/`, `groundwork-designer/`) | the three discipline-expert persona skills (canon lives in their own `references/`) | `groundwork-architecture/phases/02-technical-constraints.md`, `03-service-design.md`, `04-data-flow-communication.md` (pin architect reference filenames per constraint area — e.g. `security-and-trust.md`, `core-and-boundaries.md`); `groundwork-product-brief/instructions.md` Step 0 (pins product persona reference filenames); `groundwork-bet/workflows/01-discovery.md`, `02-design.md`, `04-delivery.md` (pin architect/product/designer reference filenames at their decision points). Renaming or splitting a persona reference file must update every pin above in the same change |

