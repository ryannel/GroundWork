"""Stage 4: crews can see what is still pending."""

import pytest

pytestmark = pytest.mark.skip(reason="Stage 4 not reached — sync status not implemented")


def test_pending_count_reflects_queue(tmp_path):
    raise NotImplementedError
