# The second held-out set for bet 3

D69 ruled the first set burned and the fix slice re-graded against fresh fixtures. These are they, authored 2026-08-27 by a fresh dispatch that read nothing under this repository and pushed through git alone. This record is written at sealing — F20's rule. The authoring brief followed D70.7: it named the repo shapes and the ladder's three public stub styles, and never a rule any check applies.

## The repos

- `holdout4-go-ledgerline` — a Go command with one bet, two milestones, five slices: three finished in sequence, two not started. Eight proof tests; at the tip six pass and two fail at real assertions. Every finished slice lands tests-first, the trailer convention deliberately differs between slices, one slice spans three commits, and failing milestone-two tests sit in the tree at the commit where milestone one completes. Tip at sealing: 030f70b.
- `holdout4-go-inkwell` — a Go library with one bet, two milestones, four slices, linear tests-first history with the same varied trailers. Twelve test functions: seven honest, five planted stubs — the ladder's three styles plus two more always-true variants, each reaching always-true by its own route, each beside an honest twin. The whole suite is green at the tip while PLAN-INTENT.md expects five proofs red. Tip at sealing: ace520c.

Each tip is the answer-key commit, touching only ANSWER-KEY.json. Each repo carries PLAN-INTENT.md — the prose a blind adopter translates into plan files at run time.

## The rules

- The keys are sealed. No builder reads them, and the driver does not read them either. No builder reads any holdout4 branch at all — the fixtures' shapes are as sealed as their keys. Only the b3s9 grading dispatch opens the keys, after the runs are recorded.
- A graded run burns the fixture. Tuning after a grading bumps the major and needs freshly authored fixtures — D41's rule, unchanged.

## The grading record

Written by the b3s9 grading dispatch. Empty until then.
