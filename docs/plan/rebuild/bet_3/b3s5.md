---
id: b3s5
bet: bet_3
milestone: m_b3_board
proofs:
  - id: b3s5_stub
    marker: TestProof_b3s5_stub_a_proof_expected_red_must_fail_at_an_assertion
    from: docs/evidence/bet-3/design.md#r10--expected-state-and-the-stub-check-b19-k14
    headline: true
    retire_at_close: false
fixtures:
  - a proof that passes, one that skips, one that fails to build, and one that dies before its assertion
  - an honest red beside each of them
real:
  - the honesty scan's own vacuous-assertion code, called rather than copied
faked: []
facing:
  - f_stub_row
records: []
---

# Slice 5 — The stub check: red for the right reason

A proof the plan expects red has to fail at a real assertion. Passing, skipping, failing to build, or dying before the assertion each fail the row, with the reason named.

The check calls the honesty scan's own code for the vacuous-assertion judgment. A second definition of vacuous would drift from the first, and this ledger already knows that class.

This slice is graded in slice 8 against sealed fixtures it never saw. Its own tests use separate, openly-authored cases.
