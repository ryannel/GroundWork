from typing import Protocol, runtime_checkable


@runtime_checkable
class LLMGateway(Protocol):
    """Port: large-language-model text generation.

    This interface is the stable contract the domain depends on. Concrete
    adapters live in src/provider/llm_gateway.py and are swappable at scaffold
    time (anthropic | openai | local | none) without touching this port or its
    callers — that is the whole point of the hexagonal boundary.
    """

    async def generate_text(self, prompt: str, max_tokens: int = 100) -> str:
        ...
